import sliderImg from '../assets/img/dark-fibo-img.webp'

type slider = string;

const sliders: slider[] = [
  sliderImg, 
  sliderImg, 
  sliderImg, 
  sliderImg, 
  sliderImg, 
  sliderImg
]

export default sliders;